# 移交包验证记录

验证日期：2026-09-03

## 已完成检查

- 从独立移交目录导入公共包成功。
- `InstrumentInteractionModule.health_check(require_loaded=True)` 通过。
- MiniCPM-V 4.6 共779项权重从移交目录成功加载到CUDA。
- MiniCPM权重、YOLO人员权重、YOLO-Pose权重和仪器标定文件均存在。
- Python 3.11.9、PyTorch 2.11.0+cu128、Transformers 5.16.1等关键版本与开发验证环境一致。
- 公共接口单元测试5项全部通过。
- `process_frame()` 非阻塞队列、外部身份匹配、无外部身份兜底和摄像头查询测试通过。

## 以前完成的真实视频回归

- 高速离心机操作视频：产生1次开始事件和1次结束事件。
- 人员路过/停留视频：0次开始事件。
- 人员站在离心机前看手机视频：0次开始事件。
- 正样本按原始帧率投递828帧，测试中0帧因队列满而丢弃。

## 尚未覆盖

- 接入方电脑环境。
- 两个摄像头同时长期运行。
- 连续24小时稳定性。
- 视角一所有仪器以及视角二部分仪器的真实正负样本。

这些未覆盖项不阻塞接口接入，但正式部署前应在目标电脑继续验证。

